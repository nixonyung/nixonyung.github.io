{
  "name": "nixonyung.github.io",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "hugo server --bind=0.0.0.0 --buildDrafts --buildFuture --disableFastRender --noHTTPCache --navigateToChanged",
    "build": "hugo --minify",
    "serve": "pnpm build && http-server"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.0.12",
    "@tailwindcss/typography": "^0.5.16",
    "daisyui": "^5.0.0",
    "tailwind-scrollbar-hide": "^2.0.0",
    "tailwindcss": "^4.0.12",
    "tailwindcss-animate": "^1.0.7"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "prettier": "^3.5.3",
    "prettier-plugin-go-template": "^0.0.15",
    "prettier-plugin-tailwindcss": "^0.6.11"
  }
}
